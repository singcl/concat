{
  "name": "@singcl/concat",
  "version": "0.2.0",
  "description": "concat files based on stream.",
  "source": "concat.js",
  "main": "dist/concat.js",
  "module": "dist/concat.m.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "tsc && microbundle",
    "release": "npm run -s bundle && git add --all && git commit -am %npm_package_version% && git tag %npm_package_version% && git push && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/singcl/concat.git"
  },
  "keywords": [
    "concat",
    "files",
    "stream"
  ],
  "author": "singcl",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/singcl/concat/issues"
  },
  "homepage": "https://github.com/singcl/concat#readme",
  "devDependencies": {
    "microbundle": "^0.4.4"
  },
  "dependencies": {
    "@types/node": "^10.0.8",
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  }
}
