{
  "name": "@singcl/concat",
  "version": "0.1.2",
  "description": "concat files based on stream.",
  "source": "concat.js",
  "main": "dist/concat.js",
  "module": "dist/comcat.m.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "microbundle",
    "release": "npm run -s bundle && git add --all && git commit -am :bookmark:%npm_package_version% && git tag :bookmark:%npm_package_version% && git push && git push --tags && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/singcl/concat.git"
  },
  "keywords": [
    "concat",
    "files",
    "stream"
  ],
  "author": "singcl",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/singcl/concat/issues"
  },
  "homepage": "https://github.com/singcl/concat#readme",
  "devDependencies": {
    "microbundle": "^0.4.4"
  }
}
